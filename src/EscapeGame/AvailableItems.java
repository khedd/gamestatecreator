package EscapeGame;

/**
 * Lists the items in each room as enum classes
 */
public class AvailableItems {
    public enum FirstRoom {
        MAKE_UP, DOOR_HANDLE, SCREW, COMBINED_DOOR_HANDLE, TV, USED_MAKE_UP, USED_COMBINED_DOOR_HANDLE
    }
}

/**
 * Actions that are available in an escape games
 */
class EscapeGameAction {
    enum Option{
        USE, COMBINE, PICK, DISMANTLE,
        SELECT, ZOOM, START, HOME, BACK,
        SELECT_USE, SELECT_COMBINE, SELECT_DISMANTLE,
        SELECT_EXIT, EXIT
    }
}